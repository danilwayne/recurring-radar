# Supabase Configuration
VITE_SUPABASE_URL=https://seu-projeto.supabase.co
VITE_SUPABASE_ANON_KEY=seu-anon-key-aqui

# Groq — não usado diretamente no frontend.
# A integração será feita via Supabase Edge Function (supabase/functions/radar-brief).
# Quando a Edge Function estiver pronta, adicionar a chave lá, não aqui.
# GROQ_API_KEY=  ← variável de ambiente da Edge Function, não do Vite
